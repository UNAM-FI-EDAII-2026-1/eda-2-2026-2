name: Práctica 03

on:
  push:
    branches: [ main, master ]
    paths:
      - 'practicas/practica_03/**'
      - '.github/workflows/practica_03.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'practicas/practica_03/**'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
    
    - name: Configurar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov pytest-timeout
    
    - name: Ejecutar tests
      run: |
        cd practicas/practica_03
        pytest tests/ -v \
          --cov=. \
          --cov-report=term-missing \
          --cov-report=html \
          --tb=short
      timeout-minutes: 5
    
    - name: Subir reporte de cobertura
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: coverage-practica-03
        path: practicas/practica_03/htmlcov/
        retention-days: 30
